# 打个卡验收记录

日期：2026-09-01

说明：`实现核对` 表示源码或自动化测试覆盖了该规则，不等同于真实 Windows UI/系统行为测试；`未实机验证` 表示本轮没有实际执行该人工验收动作。

## 自动化与发布证据

- [x] `dotnet test`：48/48 通过，失败 0，跳过 0。
- [x] `dotnet build -c Release`：0 警告，0 错误。
- [x] win-x64 self-contained single-file publish：退出码 0，trim 关闭，调试符号关闭。
- [x] 发布目录只有 `打个卡.exe`。
- [x] 当前 UI 改造版 EXE：`release\打个卡\打个卡.exe`。
- [x] 发布程序集内含 Edge 86 帧、Gate 50 帧透明 PNG，发布时不依赖 MP4、FFmpeg 或 Python。
- [x] EXE 大小：124,299,406 字节。
- [x] SHA256：`AB5504FA4991FBD7C70B72BB5F7930503B3A8FF0F19F305C7A23FD4894E5EE68`。
- [x] EXE 的 ProductName / FileDescription 均为“打个卡”，并可提取嵌入图标。

## A. 启动/托盘

- 正常启动无主窗口：实现核对；未实机验证。
- 任务栏无常驻窗口：实现核对；未实机验证。
- 托盘有图标：实现核对；未实机验证。
- 双击托盘打开设置：实现核对；未实机验证。
- 右键只有“设置 / 退出程序”：实现核对；未实机验证。
- 托盘右键菜单使用黑黄暖白自定义渲染：实现核对；未实机验证。
- 第二次启动不产生第二实例：Named Mutex 实现核对；未实机验证。
- 托盘退出始终直接成功：退出清理实现核对；未实机验证。

## B. 配置

- 默认 09:30 / 10:00 / 5 / 19:00 / 10：实现核对；未实机验证。
- 上午时间可改：实现核对；未实机验证。
- 上午间隔可改：实现核对；未实机验证。
- 晚上时间可改：实现核对；未实机验证。
- 晚上间隔可改：实现核对；未实机验证。
- 5/10/15/30 快捷值存在：实现核对；未实机验证。
- 自定义整数有效：1–1440 校验实现核对；未实机验证。
- 未点击保存时不生效：编辑副本实现核对；未实机验证。
- 保存后生效：统一保存回调实现核对；未实机验证。
- 重启程序设置仍在：JSON 持久化实现核对；未实机验证。
- Settings X 不退出程序：窗口生命周期实现核对；未实机验证。
- 设置窗口标题为“打个卡设置”，默认客户区为 900×680，可双向缩放和最大化，最小窗口为 780×560；中间双栏内容在高度不足时纵向滚动，顶部与底部操作区保持可见：真实 WinForms 句柄回归测试通过；待用户目视确认。
- “开机自启动”卡片的标题、副标题和开关完整显示；开关与“久坐提醒”采用相同标题行高度、垂直位置和右侧对齐线：真实 WinForms 坐标回归测试与窗口截图核对通过。
- 时间与间隔输入使用纯自绘胶囊控件；点击后弹出码表式滚轮选择器：实现核对；滚动、拖动与吸附手感待用户目视确认。
- 卡片标题区根据标题和副标题的实际渲染高度自测量：实现核对；100% 与 125% DPI 下文字完整性待实机确认。
- 右侧播放当前角色真实 Edge 动画预览；前后按钮在单角色时自动禁用：实现核对；待用户目视确认。
- “使用这个角色”明确确认预览角色，“保存全部设置”统一持久化所有设置：状态模型自动化测试通过；待用户目视确认。
- 旧配置缺少角色字段时自动选择 `white-bear`：自动化迁移测试通过。
- 久坐提醒默认关闭，默认 09:00–18:00、60 分钟：自动化测试通过；设置页未实机验证。
- 久坐详情始终显示，开关仅控制详情启用或置灰，不再改变卡片高度：实现核对；零闪屏待实机确认。
- 久坐间隔仅允许 1 / 1.5 / 2 / 3 小时：校验与自动化测试通过；未实机验证。
- 旧六字段 JSON 自动保留原值并补齐久坐默认值：自动化迁移测试通过。
- 保存成功后按钮显示“已保存 ✓”，750ms 后自动关闭设置窗体：实现核对；未实机验证。

## C. AutoStart

- 第一次默认开启：实现核对；未实机验证。
- HKCU Run 指向当前 EXE：`Environment.ProcessPath` 与实际值比较实现核对；未实机验证。
- 关闭 AutoStart 后 Run value 删除：实现核对；未实机验证。
- 再打开可恢复：实现核对；未实机验证。
- 登录 Windows 后静默启动：未实机验证。

## D. 第一层边缘动画

- 主屏上、下、左、右随机出现：四边枚举、随机选择和 WorkingArea 定位实现核对；未实机目视验证。
- 第一段保持 7.104 秒总时长，不压缩到 5 秒：时间轴契约测试通过。
- 绿幕已转换为 86 帧软边透明 PNG：资源数量测试与逐帧解码抽查通过。
- 角色使用 `UpdateLayeredWindow` 逐像素透明、置顶、点击穿透：实现核对；透明边缘需实机目视验证。
- 气泡在角色出现 2.1 秒后显示；打卡文案及“已打卡，闭嘴”完全正确：文案契约测试通过；未实机验证。
- 久坐气泡没有按钮、点击穿透并随动画自动关闭：实现核对；未实机验证。
- 第一层不抢正在输入的焦点：`ShowWithoutActivation`、`WS_EX_NOACTIVATE`、`MA_NOACTIVATE` 实现核对；必须用记事本实测。
- 无 X、无声音、动画结束自动消失：实现核对；未实机验证。
- 打卡可打断久坐；冲突久坐最多保留一次并在打卡流程后补显示：队列自动化测试通过；完整 UI 未实机验证。

## E. 上午

- 09:30 开始：自动化时间计算通过；完整流程未实机验证。
- 默认 5 分钟固定节点：自动化测试通过；未实机验证。
- 10:00 不再弹：排他边界自动化测试通过；未实机验证。
- 点击后直接结束且不出现第二道：实现核对；未实机验证。
- 09:47 启动立即弹，之后回 09:50：固定节点自动化测试通过；启动 UI 未实机验证。
- 未确认也在 MorningEnd 自动停止：实现核对；未实机验证。

## F. 晚上

- 19:00 开始、默认 10 分钟固定节点、无结束时间：实现核对；未实机验证。
- 第一层点击后出现第二道：实现核对；未实机验证。
- 第二道文案及“真的 / 假的”完全正确：实现核对；未实机验证。
- “真的”为黄底黑字，“假的”为白底黑字，均使用显式高对比样式：实现核对；未实机验证。
- 第二道无 X、不自动关闭、出现时抢焦点：实现核对；未实机验证。
- 第二段为 50 帧透明动画，保持 4.086009 秒源时长，播完从第一帧重新开始且不倒放：时间轴与资源契约测试通过；未实机目视验证。
- Alt+F4 不能绕过：`FormClosing`/`allowClose` 实现核对；未实机验证。
- “假的”不完成且回原固定时间轴：实现核对；19:13 -> 19:20 自动化时间计算通过；完整流程未实机验证。
- “真的”停止晚间提醒：实现核对；未实机验证。

## G. 状态不持久化

- 上午/晚上完成后退出再启动会重新提醒：两个状态仅为进程内字段；未实机验证。
- config 只保存十一项用户设置（原十项加角色 ID），不保存打卡、久坐状态、日期、动画位置或历史：模型实现核对。

## H. Test Reminder

- 测试按钮立即显示真实边缘动画并可进入动画第二道闸门：显式 `ReminderKind.Test` 实现核对；未实机验证。
- 测试“真的/假的”不改变正式状态、正式 next due 或 shutdown block：实现核对；未实机验证。

## I. Resume / Unlock

- 上午/晚上有效窗口 Resume 或 Unlock 立即补一次：实现核对；未实机验证。
- 同一次恢复不重复叠加：中央协调实现核对；未实机验证。
- 补完后回固定时间轴：实现核对；未实机验证。

## I.1 久坐固定节点

- 09:00 启动锚点、09:00–18:00 排他窗口：自动化测试通过。
- 60 / 90 / 120 / 180 分钟均从开始时间保持固定节点：自动化测试通过。
- 当天最后节点后自动计划次日开始时间，不会在 18:00 后永久停止：跨日回归测试通过。
- 关闭开关会停止未来调度并取消打卡流程后尚未显示的排队久坐：队列测试通过。

## J. Shutdown / Restart

- 晚间未完成时普通 shutdown/restart veto：实现核对；未实机验证。
- 关机守护窗体不会被普通 `Close` 提前销毁，明确退出时仍会释放；已释放对象收到排队中的注册更新不会再访问 `Handle`：生命周期回归测试通过。
- Windows blocker UI 显示合理原因：proactive reason 注册实现核对；未实机验证。
- 返回桌面后显示/恢复提醒流程：实现核对；未实机验证。
- 第二道未点“真的”前继续 veto，“真的”后放行：实现核对；未实机验证。
- Logoff / ENDSESSION_CLOSEAPP / Critical 路径放行：位掩码实现按 Microsoft 官方定义核对；未实机验证。
- 托盘退出后 shutdown/restart 正常：清理实现核对；未实机验证。
- 工具不自动替用户重新关机：源码未包含关机调用；未实机验证。

## K. 发布

- [x] `dotnet test` 通过。
- [x] Release build 通过。
- [x] win-x64 self-contained publish 通过。
- [x] 发布目录拿到单个最终 EXE。
- [x] 最终 EXE 含 136 帧动画资源且 ProductName / FileDescription 均为“打个卡”。
- [ ] Windows 11 实机运行：未实机验证。
- [ ] 公司 Windows 10 实机运行：未实机验证。
- [ ] 公司 Windows 10 AutoStart：未实机验证。
- [ ] 公司 Windows 10 NoActivate 边缘动画与气泡：未实机验证。
- [ ] 公司 Windows 10 shutdown blocker：未实机验证。
- [ ] 程序运行时没有主动网络请求：源码未包含网络 API；运行时未实机验证。

## 当前未执行人工测试的原因

本轮已启动新发布版并验证进程从 `release\打个卡\打个卡.exe` 正常驻留。该程序只有托盘入口，不暴露可绑定的普通窗口；Codex Windows Computer Use 也无法安全绑定任务栏通知区域，因此没有猜测坐标点击托盘。设置窗口已通过真实 WinForms STA 线程完成创建句柄、双向改变尺寸和最小高度滚动范围验证；透明动画、按钮手感、焦点和循环仍保留为“未实机验证”。文档明确禁止无人值守触发真实关机/重启，所以结束会话相关项目同样只做实现核对。
