> 历史阶段记录：可能包含已被后续版本替代的界面、限制和路径。当前使用与验收以 [README](../README.md) 和 [VERIFICATION](../VERIFICATION.md) 为准。

# 自定义角色创建试用版验证 · 2026-09-19

## 交付

- 本地试用版：`G:\打卡项目\release\v1.2.0-preview.1\DaGeKa-v1.2.0-preview.1-win-x64.exe`
- win-x64 / self-contained / single-file / PublishTrimmed=false。
- 大小 247,904,418 字节；包含离线视频组件，首次导入解压至用户目录。
- SHA256：`1360CD0942310A8767CFBFEACA7F64A07BAC6AA9585593CD32558CF8A543724D`。
- 同目录提供试用说明、完整示例素材和素材使用说明。未覆盖 v1.1.0，未上传 GitHub 或创建新 Release。

## 已执行检查

- 基线原 229 项测试通过。
- 新增 15 项测试通过：角色包重载、非法路径、坏包隔离、失效角色 ID 保留其他设置、向导空名称提示、图层渲染、统一视频裁切、取消回滚、完整 MP4 导入提交、姿势画布一致、透明 WebM 保留透明及绿色角色、自定义画廊按需加载。
- 离屏 WinForms 创建向导四步截图，以及鼠标连接点编辑、鼠标极限点击、键盘与鼠标组合图已输出并检查。
- 真实生成 MP4 → 内嵌工具解码 → 去绿幕 → 帧统一裁切 → 本地包校验和桌宠加载通过。
- 自包含单文件验收工具在子进程 PATH 为空、DOTNET_ROOT 指向不存在目录的情况下完成同一导入与界面检查；不依赖外部 ffmpeg 或外部 .NET。验收工具不启动提醒调度和全局输入钩子。
- 最终完整 Release 测试：244 通过 / 0 失败 / 0 跳过，耗时 3 分 1 秒，TRX 已保存。第三方许可契约修正后再次完整运行通过。

## 修复记录

- 自定义鼠标网格三角拼接产生裂缝：像素回归复现后，改为连续逆向采样，保持肩部固定。
- `InvalidDataException` 不属于 `IOException`：为向导和角色目录增加明确处理，语义损坏包隔离测试通过。
- 失效角色 ID 导致整份设置回到默认：改为只回退角色，并在删除当前角色时同步回退活动状态。
- 新按钮挤压旧画廊：宽窗口按钮与标题同排，原五角色无需滚动的契约继续通过。
- 第三方许可窗口应与嵌入全文完全一致：许可全文合并至权威文档，避免额外运行时拼接。

## 验证边界

没有运行真实关机或重启。没有更改用户当前角色、开机启动或提醒时刻。桌面真实键盘短按/长按、鼠标跟随手感、多显示器及多种 DPI 仍需实机体验；离屏截图与自动测试不代替该验收。

素材能力边界：不生成新动作，不自动从复杂背景抠角色，不自动拆任意图片的手臂，不补画被遮挡区域。完整鼠标互动需要干净底图和独立透明手臂。去纯色可能影响角色同色部分，应换另一种纯色或透明素材并预览。

## 证据位置

`G:\打卡项目\release\verification-custom-characters\`

- `test-results/custom-characters-final.trx`
- `creator-step-1.png` 至 `creator-step-4.png`
- `creator-mouse-editor.png`
- `mouse-right-click.png`、`key-left-mouse-left.png`
- `standalone/`：独立单文件验收输出
